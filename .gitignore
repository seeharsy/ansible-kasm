backup/
*.tar.gz
*.tar

# Inventory contains host IPs — keep local
inventory

# Vault contains plaintext credentials — encrypt before committing
group_vars/all/vault.yml

# Generated credential files — load into vault, do not commit plaintext
kasm_credentials_*.yml

# Ansible-galaxy installed artifacts — not source-controlled
.ansible/
